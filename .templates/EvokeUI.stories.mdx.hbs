import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';

import { {{pascalCase name}} } from './{{pascalCase name}}.component';

<Meta title="{{pascalCase atomic}}/{{pascalCase name}}" component={ {{pascalCase name}} } />

# {{pascalCase name}}

Let's define a story for our `{{pascalCase name}}` component:

<Canvas>
  <Story name="{{pascalCase name}}">
    <{{pascalCase name}}>I am {{pascalCase name}}</{{pascalCase name}}>
  </Story>
</Canvas>

<ArgsTable of={ {{pascalCase name}} } />
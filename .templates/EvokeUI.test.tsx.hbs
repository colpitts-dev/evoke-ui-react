import React from 'react';
import { render, cleanup } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';

import { {{pascalCase name}} } from './{{pascalCase name}}.component';

afterEach(cleanup);

describe('{{pascalCase name}}', () => {
  it('renders without error', () => {
    const { getByTestId } = render(<{{pascalCase name}} data-testid="{{pascalCase name}}Component">Foo Bar Baz</{{pascalCase name}}>);
    const component = getByTestId('{{pascalCase name}}Component')

    expect(component).toBeInTheDocument();
  });
});